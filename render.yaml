services:
  - type: web
    name: school-data-dash
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn wsgi -c gunicorn_config.py
    envVars:
      - key: PYTHON_VERSION
        value: "3.11.6"
